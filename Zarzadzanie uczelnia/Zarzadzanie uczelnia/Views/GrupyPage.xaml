<Page x:Class="Zarzadzanie_uczelnia.GrupyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="GrupyPage">

    <Grid Background="#F5F7FA" Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Zarządzanie grupami"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <Border Grid.Row="1" Padding="15" Background="White" CornerRadius="10" Margin="0,0,0,10">
            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="5">
                    <TextBlock Text="Nazwa grupy" FontWeight="SemiBold"/>
                    <TextBox Width="200"
                             Height="30"
                             Text="{Binding NazwaGrupy}"/>
                </StackPanel>

                <Button Grid.Row="0" Grid.Column="1"
                        Content="Dodaj grupę"
                        Height="40"
                        Margin="10,20,0,0"
                        Background="#4CAF50"
                        Foreground="White"
                        FontWeight="Bold"
                        BorderThickness="0"
                        Click="Add_Group"/>

            </Grid>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="10">
            <DataGrid ItemsSource="{Binding Grupy}"
                      AutoGenerateColumns="True"
                      IsReadOnly="True"
                      VerticalAlignment="Stretch"
                      HorizontalAlignment="Stretch"/>
        </Border>

    </Grid>
</Page>